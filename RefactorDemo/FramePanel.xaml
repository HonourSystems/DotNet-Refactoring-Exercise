<UserControl x:Class="RefactorDemo.FramePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="60" Width="60">
    <Border BorderBrush="Black" BorderThickness="2">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label x:Name="First" Content="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Label x:Name="Second" Content="2" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1"/>
            <Label x:Name="Third" Content="3" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1"/>
            <Label x:Name="Total" Content="T" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Grid.Row="1"/>
        </Grid>
    </Border>
</UserControl>
